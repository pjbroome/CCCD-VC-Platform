# Environment
.env
__pycache__/
*.pyc
.venv/

# Large binary files (use generate_embeddings.py to rebuild)
rag_embeddings.npy
rag_embeddings.npz
rag_embeddings.json

# Training data (stored in cccd-training-architect repo)
training_data/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# VC slide images (119MB — too large for git, bundled in Docker build)
app/vc_slides/slide_images/

# VC PHI data — patient intake + consultation records. NEVER commit (PII/PHI).
# Real data lives on the Fly /data volume at runtime. See *.example for schema.
app/vc_slides/vc_requests.json
app/vc_slides/vc_consultations.json
rag_embeddings.npy
rag_chunks_meta.json
rag_chunks_text.jsonl
rag_chunks_text_offsets.json
rag_inverted_index.json
/tmp/rag_embeddings.json
