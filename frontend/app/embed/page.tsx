import { VCIntake } from "@/components/vc/vc-intake"

// Iframe-embed target for the website widget. Same intake form, no extra chrome.
export default function EmbedIntake() {
  return <VCIntake />
}
